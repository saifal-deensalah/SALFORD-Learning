# مراجعة التسجيل والتصميم — 2026-08-28

## لماذا تختلف الصورة عن Figma؟

`SessionApp` في `frontend/src/App.tsx` يعرض `LearningApp` للزائر، لكنه ينتقل إلى `StudentApp` عند وجود جلسة طالب. الأول يستخدم الشاشات الأصلية المستخرجة بواسطة `DesignScene`، والثاني يستخدم `NativeUI` بتصميم مبسّط لعرض بيانات API. لذلك الصورة المرسلة للصفحة الرئيسية لا تطابق `frontend/design/screens/1066.png`: العنوان والبحث والبطاقات الأفقية والأبعاد والتنقل مختلفة. اختلاف البيانات وحده لا يفسر اختلاف التصميم.

لم أستبدل StudentApp بعرض Figma التجريبي لأن ذلك سيفقد ربط وظائف الطالب الفعلية. إعادة تصميم واجهة الطالب المتصلة كاملة عمل مستقل لم يُنفّذ هنا. التعديل الحالي يخص التسجيل والدخول وإزالة شرط تفعيل البريد في الوضع المحلي فقط.

## التصميم المعتمد

الأصل متاح في `frontend/design/source/canvas.fig`، وصورتا Login وSign up في `frontend/design/screens/818.png` و`985.png`، مع عقد التخطيط في `frontend/src/design/screens.json`. تم فحصهما بصريًا قبل التعديل. حافظت الشاشتان على خلفية Figma والحقول وترتيبها وأزرارها وأبعادها الأصلية.

الاستثناءات المقصودة: خفض وضوح أزرار Google/Apple وتعطيل لمسها مع توضيح أنها غير مهيأة؛ وتصحيح رابط أسفل Sign up من «Joined us before? Sign Up» إلى «Joined us before? Log In» لأن وجهته الحساب الموجود. لم تُضف خانة اسم غير موجودة في التصميم. لا أدّعي تطابقًا بصريًا100% أو مطابقة باقي التطبيق.

## سياسة المصادقة

- `LOCAL_EMAIL_AUTH=true` صريح في `.env` المحلي، مقبول فقط في development مع embedded وloopback؛ يُرفض في production أو PostgreSQL الخارجي أو HOST عام.
- حساب جديد حقيقي، Argon2id، ثم login بكلمة السر عبر API؛ لا جلسة أو نجاح وهمي. التكرار409، كلمة السر الخاطئة401، حقول role إضافية422، الطالب لا يصل للأدمن403.
- الحسابات القديمة غير المفعلة تعمل دون حذف أو إعادة إنشاء أو تغيير `email_verified_at` أو hash. `emailVerified` يظل صادقًا، والسياسة منفصلة في `emailVerificationRequired`.
- لا email challenge أو outbox للتسجيل المحلي؛ لا رسائل تفعيل في التسجيل أو الملف الشخصي أو زر إعادة إرسال التفعيل في checkout المحلي.
- استعادة كلمة السر ليست ملغاة؛ البريد المحلي ملف وليس رسالة تصل للمستخدم. Google/Apple غير مهيأين؛ لا تغيير في Supabase أو البيانات الخارجية.

## الملفات

backend: `core.ts` للإعداد والحارس، `auth.ts` للتسجيل والاستجابات، و`catalog.ts` و`learning.ts` و`billing.ts` و`demo-billing.ts` لتمرير السياسة إلى حارس التفعيل فقط دون تغيير قيود الوصول الأخرى؛ عقد OpenAPI، `.env` المحلي و`.env.example`، اختبارات API، وأداة `scripts/check-local-auth.mjs`.

frontend: `App.tsx` لمسار التسجيل وتعطيل OAuth، `DesignScene.tsx` لدعم زر معطّل، `form-fields.ts` لحدود الحقول المطابقة للعقد، `styles.ts`، نوع المستخدم ورسالة الإيميل المكرر في `services/api.ts`، وإخفاء رسائل التفعيل المحلية في `StudentApp.tsx` و`DemoCheckout.tsx`، واختبارات الواجهة. لم يُغيّر تخطيط Home أو بيانات الدورات.

الملفات السابقة محفوظة في `backendCSC/.local/auth-review/source-backup-1787932500747`، خارج Git. لا يوجد مستودع `.git` في النسخة الحالية. لم تُمسح بيانات أو ملفات تطبيق.

## التحقق

نجحت33 حالة backend على قاعدة معزولة مع HTTP فعلي؛ تشمل الحارس المحلي، الحساب القديم غير المفعّل، Argon2id، عدم إنشاء بريد، تسجيل الدخول والخروج، كلمة السر الخاطئة، الإيميل المكرر، رفض suspended والتصعيد للأدمن، ومسارات الحماية الحالية. نجح بناء backend وTypeScript للـbackend وfrontend. نجحت59 حالة frontend في4 مجموعات، وتشمل التسجيل المحلي ومسار البريد الافتراضي، فشل الدخول بعد إنشاء الحساب، الإيميل المكرر، حقول Figma وتنقلها، تعطيل OAuth وإخفاء رسائل التفعيل في الملف الشخصي المحلي. ESLint: صفر أخطاء و17 تحذيرًا سابقًا. فحص OpenAPI نجح لكل78 عملية.

فُحص تركيب `check-local-auth.mjs` بنجاح. محاولة تشغيله والسيرفر يعمل رُفضت قبل فتح القاعدة أو إنشاء الحسابات، كما هو مقصود. بعد أن أوقف المستخدم السيرفر بـCtrl+C، نجح الفحص كاملًا على القاعدة الحالية `backendCSC/.local/local-app/postgres` وعلى `127.0.0.1:3000`. شمل التسجيل202، التكرار409، كلمة سر خاطئة401، الدخول للحساب الجديد ولحساب اختبار يحاكي القديم غير المفعّل200، رفض /me دون جلسة401، رفض أدمن للطالب403، والخروج204 وإلغاء الجلسة401. أُوقفت عملية backend الاختبارية طبيعيًا ثم بدأت عملية جديدة؛ بقي نفس الحساب ونفس بياناته ونجح الدخول مجددًا.

أكد الفحص Argon2id وكلمة السر، عدم تعديل سجل الحساب غير المفعّل، عدم تغيير سجلات المستخدمين الآخرين، وعدم إنشاء challenge أو رسالة تفعيل للحسابين. لم تُشغّل migrations أو seed أو خدمات خارجية. بقي حسابا الاختبار ولم يُحذفا؛ بياناتهما الخاصة والتقرير في `backendCSC/.local/auth-review/fcab40e2-2e78-4ad5-b724-38a1efbf0f5a/`. انتهت عمليتا الاختبار، وأُعيد تشغيل backend العادي بالإعداد الجديد. فحص الحالة المحفوظ في `result.json` نجح بتاريخ2026-08-28T16:32:32Z.

## اختبار Android الفعلي

نجح على `Pixel_8 / emulator-5554` مع API المحلي3000 وMetro8082:

1. فُحصت شاشتا Login وSign up بصريًا، والحقول الثلاثة ورابط الرجوع؛ أزرار Google/Apple تحمل `enabled=false` وتوضيح عدم الإعداد.
2. أُدخل إيميل حساب اختبار ثالث وكلمة السر وتأكيدها في حقول Sign up الأصلية، ثم ضُغط Create account. سجل backend طلب register202 ثم login200؛ انتقلت الواجهة إلى `student-app` دون شاشة أو رسالة تفعيل.
3. عُرض الملف الشخصي لنفس الحساب دون طلب تفعيل أو إعادة إرسال أو ادعاء Email verified.
4. ضُغط Log out؛ سجل backend204، وعادت شاشة818 بحقول فارغة. أُدخلت بيانات نفس الحساب في Login، وسجل backend200 ثم getHome200 وظهرت صفحة الطالب مجددًا.

صور الدليل تحت `frontend/artifacts/auth-review`: `signup-final.png` و`login-final.png` و`signup-success.png` و`profile-no-activation.png` و`login-success.png`. كلمة السر غير مطبوعة، والحسابات الثلاثة محفوظة محليًا ولم تُحذف.

ظهر أولًا تعليق في Metro: status/inspector كانا يستجيبان لكن تحميل JavaScript لم يكتمل. المشغّل أعاد تشغيل Metro الموثق فقط دون reset-cache، ثم أُعيد فتح عملية التطبيق دون إعادة تثبيت أو مسح بيانات. رجع bundle200 بحجم9,498,913بايت. ظهرت أيضًا مهلة ADB في محاولة المشغّل؛ لم نعتبرها نجاحًا، وأثبتنا نجاح التطبيق لاحقًا من الواجهة وطلبات السيرفر. إدخال النص السريع بـADB أسقط أحرفًا؛ أُعيد الإدخال بتأنٍ وتحقّق اكتمال الحقول قبل التسجيل. لم يُغيّر عنوان Metro المحفوظ.

حدود الاختبار: كلمة السر الخاطئة والإيميل المكرر والصلاحيات وبقاء الحساب بعد إعادة تشغيل backend اختُبرت عبر HTTP الفعلي؛ لم تُكرر كل حالات الفشل باللمس على Android. لم يُختبر iOS أو OAuth الحقيقي، ولم تُعد مطابقة تصميم بقية StudentApp. استعادة كلمة السر وتغيير الإيميل خارج تعديل السياسة المحلية؛ اختبارات backend الحالية لهما ما زالت ناجحة.
